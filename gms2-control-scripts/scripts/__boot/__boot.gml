/// @description Bootstraps everything we need before the game starts
/// @function __boot

/**
 * IMPORTANT:
 * Run this only ONCE at the start of your game.
 */

// Controls bootstrap
controlsInit();
